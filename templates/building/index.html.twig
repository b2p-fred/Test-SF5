{% extends 'base.html.twig' %}

{% block title %}Hello BuildingController!{% endblock %}

{% block content %}
    <h1>Hello buildings!</h1>

    {# List of all the items #}
    {% for item in items %}
        <ul>
            <li><a href="{{ path('building_show', {"id": item.id}) }}">{{ item.name }}</a></li>
        </ul>
    {% endfor %}
{% endblock %}
